<div style="width: 100%;">
  <div class="align-center">
      <h4>Clientes</h4>
  </div>
</div>


<div style="width: 100%;">
  <div class="block-page">

    <p id="notice"><%= notice %></p>

    <table>
      <thead>
        <tr>
          <th>NOME</th>
          <th>CPF</th>
          <th>EMAIL</th>
          <th>ENDEREÇO</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @clientes.each do |cliente| %>
          <tr>
            <td><%= cliente.nome %></td>
            <td><%= cliente.cpf %></td>
            <td><%= cliente.email %></td>
            <td><%= cliente.endereco %></td>
            <td><%= link_to '<i class="material-icons">visibility</i>'.html_safe, cliente %></td>

            <td><%= link_to '<i class="material-icons">edit</i>'.html_safe, edit_cliente_path(cliente) %></td>
            <td><%= link_to '<i class="material-icons">delete</i>'.html_safe, cliente, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    
  </div>
</div>



<%= render 'action', page: 'index'%>